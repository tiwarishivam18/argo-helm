apiVersion: v1
kind: Service
metadata:
  name: {{ include "argo-helm.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "argo-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argo-helm.selectorLabels" . | nindent 4 }}
