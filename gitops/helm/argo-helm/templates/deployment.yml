apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "argo-helm.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "argo-helm.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "argo-helm.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "argo-helm.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.targetPort }}
              protocol: TCP
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}