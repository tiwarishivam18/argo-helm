apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "arog-helm.fullname" . }}-test-connection"
  labels:
    {{- include "arog-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "arog-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
